# npm opens 15 parallel sockets by default and this registry answers quickly but
# slowly enough that a cold `npm ci` drifts into ETIMEDOUT on this machine and on
# the router. Two sockets take longer in theory and finish in practice.
maxsockets=2
