<%# "Общие" is the single settings panel now: profiles and defaults are gone, so
    there is one body to edit and no inheritance badge to explain. %>
<h2><%= panel.title %></h2>
<%- include('settings-form', {panel}) %>
