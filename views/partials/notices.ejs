<%# Errors and notices. A multi-line generation summary keeps its line breaks. %>
<% if (error) { %>
  <p class="error"><%= error %></p>
<% } %>
<% if (notice) { %>
  <p class="notice"><%= notice %></p>
<% } %>
