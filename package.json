{
  "name": "gatehouse",
  "version": "0.1.0",
  "private": true,
  "description": "GateHouse — web editor of webui.json and generator of sing-box config.json",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "start": "node src/web/server.mjs",
    "dev": "node tools/dev.mjs",
    "generate": "node tools/generate.mjs",
    "vendor:htmx": "node tools/vendor-htmx.mjs"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ejs": "^3.1.10",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "htmx.org": "^2.0.7"
  }
}
