{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/gatehouse/schemas/webui.schema.json",
  "title": "webui.json",
  "description": "Settings of the sing-box config generator. Flat since version 2: the profile level and the `defaults` object were removed (see techdocs/architecture.md §8.9). A version-1 file is migrated once by the editor.",
  "type": "object",
  "additionalProperties": false,
  "required": ["version"],
  "properties": {
    "version": {
      "description": "Schema version, bumped only on incompatible changes. 1 was the profile-level form; 2 is flat.",
      "const": 2
    },
    "note": {
      "description": "Free-form comment carried over from settings.yaml. Ignored by the core.",
      "type": "string"
    },
    "listen_ip": {
      "type": "string",
      "minLength": 1
    },
    "providers": {
      "description": "State of the discovered provider folders, keyed by the FOLDER NAME under GATEHOUSE_PROVIDERS (the provider identifier). A provider absent from this map is found and DISABLED: its servers are not in the generated config until the owner ticks it. `label` is a human-readable name shown in the editor only — it never reaches tags, keys or config.json.",
      "type": "object",
      "propertyNames": {
        "pattern": "^(?!\\.)(?!-)[A-Za-z0-9_.-]+$"
      },
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "enabled": {
            "description": "Whether the provider's servers take part in the generated config. Missing means false.",
            "type": "boolean"
          },
          "label": {
            "description": "Human-readable name shown instead of the folder name. Trimmed; shown only, never stored in tags or config.json.",
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[^\\u0000-\\u001F\\u007F]+$"
          }
        }
      }
    },
    "tunnels": {
      "description": "Tunnels prepared for use: the human-readable name and the file name of the applied `<interface>.conf` in GATEHOUSE_AMNEZIA_DIR. Written when the owner ticks «нужен» in the Providers panel; a tunnel may exist without a proxy. Additive to version 2: a document without the key stays valid.",
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["provider", "file", "name", "interface"],
        "properties": {
          "provider": {
            "description": "Provider folder the source `.conf` came from. Also the group heading of the System panel.",
            "type": "string",
            "minLength": 1
          },
          "file": {
            "description": "Source file name inside the provider folder, with the `.conf` suffix.",
            "type": "string",
            "minLength": 1
          },
          "name": {
            "description": "Human-readable name of the tunnel: how it is chosen and edited. Never reaches the kernel, hence the 255 limit of the file system rather than 15.",
            "type": "string",
            "minLength": 1,
            "maxLength": 255
          },
          "interface": {
            "description": "File name of the applied config `<interface>.conf`, the systemd instance `gatehouse-tunnel@<interface>` and the `bind_interface` of the generated outbound. Kernel limit is 15 characters.",
            "type": "string",
            "minLength": 1,
            "maxLength": 15
          },
          "policy_routing": {
            "description": "The tunnel is also handed out through 3proxy by the source address, so the normalised config carries PostUp/PreDown with `ip rule`.",
            "type": "boolean"
          }
        }
      }
    },
    "output_file": {
      "type": "string",
      "minLength": 1
    },
    "exclude_from_auto": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "urltest": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "url": {
          "type": "string",
          "minLength": 1
        },
        "interval": {
          "type": "string",
          "minLength": 1
        },
        "tolerance": {
          "type": "integer"
        }
      }
    },
    "log": {
      "description": "Passed into config.json unchanged.",
      "type": "object"
    },
    "dns": {
      "description": "Passed into config.json unchanged.",
      "type": "object"
    },
    "proxies": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["tag", "type", "port"],
        "properties": {
          "tag": {
            "type": "string",
            "minLength": 1
          },
          "type": {
            "description": "Kept in sync with PROXY_TYPES of errors.mjs; a test guards the drift.",
            "enum": ["socks", "http", "mixed"]
          },
          "port": {
            "type": "integer",
            "minimum": 1,
            "maximum": 65535
          },
          "servers": {
            "description": "Server tags. An empty or missing list means auto-select. A single string is accepted and normalised by the core, exactly as in the reference.",
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1
                }
              }
            ]
          },
          "note": {
            "description": "Free-form comment. Ignored by the core.",
            "type": "string"
          },
          "tunnel": {
            "description": "This proxy IS a tunnel: one inbound plus one `direct` outbound bound to the interface, with no server pool. Mutually exclusive with `servers` — one tunnel, one exit.",
            "type": "object",
            "additionalProperties": false,
            "required": ["provider", "file", "interface"],
            "properties": {
              "provider": {
                "description": "Provider folder the source `.conf` was taken from. Grouping label of the System panel.",
                "type": "string",
                "minLength": 1
              },
              "file": {
                "description": "Source file name inside the provider folder, with the `.conf` suffix.",
                "type": "string",
                "minLength": 1
              },
              "interface": {
                "description": "Interface name: the stem of the applied `<interface>.conf`, the unit `gatehouse-tunnel@<interface>` and the `bind_interface` of the generated outbound. Kernel limit is 15 characters.",
                "type": "string",
                "minLength": 1,
                "maxLength": 15
              }
            }
          },
          "pinned": {
            "description": "The exit of this proxy is fixed on purpose. Ignored by the core; enforced by the model: a pinned proxy may name at most one server.",
            "type": "boolean"
          }
        }
      }
    },
    "routes": {
      "description": "Domain routes layered on top of the proxies. Route names carry the same restriction as before: a name made of digits only would be reordered by JavaScript object key rules and would break the byte-for-byte match with the reference.",
      "type": "object",
      "propertyNames": {
        "pattern": "\\D"
      },
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "outbound": {
            "type": "string"
          },
          "domains": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            ]
          },
          "note": {
            "description": "Free-form comment. Ignored by the core: buildRules reads only outbound and domains.",
            "type": "string"
          }
        }
      }
    }
  }
}
